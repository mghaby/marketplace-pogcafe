<%= form_with(model: splash, local: true) do |form| %>
  <% if splash.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(splash.errors.count, "error") %> prohibited this splash from being saved:</h2>

      <ul>
        <% splash.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
